import styled from "styled-components";

export const ContentContainer = styled.main`
  padding: 20px;
  flex: 1 0 80%;

  & h1 svg {
    font-size: 36px;
    color: var(--main-sub);
    margin-bottom: -6px;
  }
`;
